How to get this running:


1. Run <code>git checkout ui</code>
2. Get the "secrets.js" file from repo owner, move it to the root of this repo
3. Get the "mongodump" from repo owner and mongorestore it into an instance of mongodb
4. Run <code>node app.js</code>

TODOs:
(As of 8/21/12)
-Parse the scraped pricing data of the textbooks
-Implement new view for a search-result
-Implement a simple design for the user-book-list
-Design/css magic so it doesn't look like a scrappy project
-Informal testing
