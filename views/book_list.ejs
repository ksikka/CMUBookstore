<% for(var i = 0; i < list.length; i++) { %>
  <div class="listing">
    <%= list[i].title %>
    <button onclick="removeFromList(this,'<%= action %>')" book_id=<%= list[i]._id %> >Del</button>
  </div>
<% } %>
